'include "holberton.h"

 /**
  * print_chessboard - Prints the chessboard
  *@a: Rows of a 2d Array
  */


void print_chessboard(char (*a)[8])
{
	int l, m;
	for (l = 0; l <= 7 ; l++)
	{
		for (m = 0; m <= 7; m++)
		{
			_putchar(a[l][m]);
		}
		_putchar('\n');
	}
}
